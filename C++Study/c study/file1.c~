#include<sdtio.h>
#include<ctype.h>
//如果读的方式打开，如果文件不存在，返回一个空指针，否则返回一个文件指针
//如果以写的的方式打开，如果文件存在，情空文件以前的内容，再写入
//                      ，如果文件不存在，先创建，在写入
/*
void main()
{
	FILE* fp=fopen("C:\\1.txt","r");
	FILE* fp2=fopen("C:\\2.txt","w");
	char ch;
	//fputc(ch,fp2);
	//strstr-->#include<sttring.h>
	//在文件指针在读取一次文件后，文件指针自动向后移动。
        while((ch=fget(fp))!=EOF)
	{
		if(isupper(ch))
		{
			tolower(ch);
		}
             fputc(ch,fp2);	
	}
}
*/
//二进制操作
void main()
{
   FILE *fr=fopen("");
   FILE *fw-fopen("");
   /*
   char ch;
   while((fread(&ch,1,1,fr))!=0)
   {
   	fwrite(&ch,1,1,fw);
   }*/
   /*
   while()
   {
   	fwrite(&ch,1,1024*1024*100,fw);	
   }
   */

   /*
    while()
   {
   	fwrite(&ch,1024*1024*100,1,fw);	
   }
   */
   
   /*
   int res;
   while((res=fread(&ch,1,1024*1024,fr)!=0)
  {
       fwrite(&ch,1,res,fw);		
  }
   */


   //fseek
   /* 
    
    fseek(fp,0,SEEK_END);  //获取文件大小
   /*
  ftell(fp); //告诉我们fp现在位置在哪
  printf("%d",ftell(fp));
  */

   /*
    1.文件分割和合并 做成函数
    */
}





